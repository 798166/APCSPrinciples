//  Will Kreidler
// 	1009
//  This is a comment
//  The setup function function is called once when your program begins
function setup() {
  var list = [4, 2, 1, 5, 7, 9];

  function swap(){
      if(list[a]>list[b]){
        //swapping code
        var temp = list[a];
        list[a] = list[b];
        list[b] = temp;
      }

      //swapping code
    }
  console.log(list);
}

function draw(){

}
